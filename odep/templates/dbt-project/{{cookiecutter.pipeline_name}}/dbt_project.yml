name: "{{ cookiecutter.pipeline_name }}"
version: "1.0.0"
config-version: 2

profile: "{{ cookiecutter.dbt_profile }}"

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]

models:
  {{ cookiecutter.pipeline_name }}:
    +materialized: table
