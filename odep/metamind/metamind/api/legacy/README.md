# Legacy API Modules

These files were quarantined on 2026-03-16 because they depend on removed
bootstrap contracts (`Bootstrap`, `get_bootstrap`) and old API/schema shapes.

Do not import these into active runtime until they are fully migrated to the
current `AppContext` architecture and `/api/v1/*` contracts.
